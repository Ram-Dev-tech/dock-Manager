name: CI

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  core:
    name: Core unit tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Test
        id: test
        continue-on-error: true
        run: |
          dotnet test tests/DockManager.Core.Tests/DockManager.Core.Tests.csproj \
            --configuration Release \
            --logger "console;verbosity=normal" > test-output.txt 2>&1
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
          cat test-output.txt

      # Build/test failures are re-emitted as workflow annotations so they are readable through the
      # GitHub API without downloading the raw log archive.
      - name: Surface failures
        if: always()
        run: |
          if [ -f test-output.txt ]; then
            grep -E "(error [A-Z]+[0-9]+|warning [A-Z]+[0-9]+|\[FAIL\]|^  Failed |Error Message:|Stack Trace:|Assert\.)" test-output.txt \
              | sed -e 's/%/%25/g' -e 's/\r//g' \
              | head -n 100 \
              | while IFS= read -r line; do echo "::error::${line}"; done
          fi

      - name: Fail when tests failed
        if: steps.test.outputs.exit_code != '0'
        run: |
          echo "Core tests failed with exit code ${{ steps.test.outputs.exit_code }}"
          exit 1

  windows:
    name: Build app (Windows)
    runs-on: windows-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore DockManager.sln

      - name: Build
        id: build
        continue-on-error: true
        shell: bash
        run: |
          dotnet build DockManager.sln --configuration Release --no-restore > build-output.txt 2>&1
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
          cat build-output.txt

      - name: Surface failures
        if: always()
        shell: bash
        run: |
          if [ -f build-output.txt ]; then
            grep -E "(error [A-Z]+[0-9]+|warning [A-Z]+[0-9]+)" build-output.txt \
              | sed -e 's/%/%25/g' -e 's/\r//g' \
              | sort -u \
              | head -n 100 \
              | while IFS= read -r line; do echo "::error::${line}"; done
          fi

      - name: Fail when build failed
        if: steps.build.outputs.exit_code != '0'
        run: |
          echo "Build failed with exit code ${{ steps.build.outputs.exit_code }}"
          exit 1
