namespace DockManager.Core.Ui;

/// <summary>Running/active state of a dock item, as shown by the small indicator dot.</summary>
public enum PinnedItemRunState
{
    NotRunning = 0,
    Running,
    Active,
}
