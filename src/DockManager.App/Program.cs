namespace DockManager.App;

/// <summary>Temporary entry point used while the WPF shell is being built out.</summary>
internal static class Program
{
    [STAThread]
    private static void Main()
    {
    }
}
